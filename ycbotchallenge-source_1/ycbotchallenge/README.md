# YCBotChallenge (Fabric client mod, MC 1.21.11)

You play on your own client, press **G**, and the bot runs: walk to the nearest mob, tap it once (the server's auto-attack does the rest), wait for the kill, move to the next. Press **G** again to stop. A HUD shows live pacing; everything is logged to JSONL for the analyze/sim tools.

Built and compiled against Minecraft 1.21.11 / yarn 1.21.11+build.6 / Fabric API 0.141.6.

## Install

Drop `ycbotchallenge-0.9.31.jar` into your `mods/` folder alongside Fabric Loader (>= 0.16) and Fabric API for 1.21.11. Client-side only — nothing needed on the server.

## Use

- **G** toggles the bot (rebind in Controls → Misc). **Shift+G** toggles sprinting on/off (saved to the config; shown on the HUD). **Ctrl+G** ignores the mob under the crosshair; **Ctrl+Shift+G** spotlights the companion egg / runs a companion visit. **Y** opens the options screen (0.9.30, below).
- HUD (top-left, 0.9.30 layout): two aligned columns with dim labels — header with the on/off state and sprint (alerts for captcha / stop / model offline take its place), `state` (what combat is doing, pack, ghosts), `dps` + eta, `money` + income/min, `plan` (0.9.33: what runs next and why, from the last eval's decision — `buy zone 4.4T · stage open 1.2s`, `save for zone 11.49SS 64% ~3m · sword 142% of gap`, `wait · new stage, 0 kill(s) so far`, `→ /zone max in 4s` while a send is queued; its age is shown past 10 s), `sword` and `zone` (each kind's next price with `~` before a ladder-predicted one, how far the balance is as a %, the ETA, `? ≥ floor` while unknown, and `tier 3/5` on the sword row from the Sword Skins menu), `rebirth` (target and ETA), `busy` (the one module with a visit running or planned), optionally `mods` (one chip per feature: green on, yellow busy, red suspended, grey off; off by default since 0.9.33 — the Y screen shows every module's state) and the other balances (`hudShowBalances`, off by default since 0.9.31), and kills with the rates. `hudAlpha` sets the backdrop, `hudShowPlan` / `hudShowModules` toggle the rows, `hudX/hudY` place it.
- Logs land in `.minecraft/ycbotchallenge-logs/events-<label>-<timestamp>.jsonl` — same schema as the mineflayer bot, so `analyze.js` and `simulate.js` from the bot project work on them unchanged (note: the toggle events are now named `bot_on` / `bot_off`).

## Config

`config/ycbotchallenge.json` (created with defaults on first run):

- `reach`, `targetRange` — tap distance and how far it will walk for a mob.
- `tapCooldownMs`, `reactionDelayMinMs/MaxMs`, `idleChancePerMinute` — humanization.
- `approachClickCpsMin/Max` (2–3), `approachClickMaxDist` (6) — anticipatory swing spam while closing in, with a vigor multiplier re-rolled every few seconds so the tempo wanders. Mostly whiffs at air, like a real player running up. Set max 0 to disable.
- `movement: false` — stand still and only tag what wanders into reach.
- `zoneMin`/`zoneMax` — `[x, y, z]` arrays bounding the farming zone (null = anywhere).
- `runLabel` — tag for the log file (`"baseline"`, `"asc6-geared"`, `"2x-souls"`, ...). Change it between experiment runs.
- `rebirthsPattern`, `sidebarCurrencies`, `ascensionChatPatterns`, etc. — the sidebar/chat parse knobs, in case your display format changes.

### Economy (upgrade loop)

Chat-driven, zero-spam, buy-or-wait. `/swordmax`, `/zone max` and `/rebirth` are **never polled** — every send is event-driven with a plausible human reason, and each goes through the humanized typing pipeline (kill-lull gating, log-normal pauses, typos).

**The balance is the sidebar.** The `5.62T ᴍᴏɴᴇʏ` row is reread every second and is the only money figure the bot uses. Money is credited per kill about a second after the boss bar vanishes (logs: kill 20:56:44.6 → row 20:56:45.5). `Reward Summary: (60s)` → ` + 17.19B Money` feeds only the income *rate* (HUD ETA). The 0.9.x money book (`/bal` seed, accrual, projection) is gone — it invented money between anchors and wrote its estimate back over the live row.

**Fail lines are gaps, not prices.** `You need 781.04B Money to purchase the next sword upgrade` is how far short you are — it shrinks as you earn (logs: 781.04B → 732.08B → 683.12B). The absolute next-tier price is `sidebar balance at fail + gap`, self-correcting on every fail. If the balance is somehow unknown at fail time the gap itself becomes the retry floor, so a kind can never latch shut.

**Evaluation triggers.** An evaluation is armed by a kill, by a sidebar money **increase** (`evalOnMoneyIncrease` — the kill credit itself, which also covers kills the client missed), and by a timer (`evalFallbackMs`, 30s) as the backstop. 0.9.6 was kill-only and sat on 8B→21B with a 1.24B sword for 108s because nothing died. Each eval waits for the board to settle (`postKillEvalDelayMin/MaxMs`, `upgradeSpendSettleMs`).

**The buy loop.** If the balance covers a known price, the bot waits a humanized notice delay (`buyNoticeDelayMin/MaxMs`, 2–8s) and types the command. A fail line updates the price and it goes quiet. Success is read from the server's own line when there is one — `You have unlocked a new sword level for 1.24B!` (one per level; `/swordmax` buys every level it can, so the **last** amount is the retry floor for the next tier) and `You have purchased new stage(s)!` — else **silence within `successSilenceMs` (3s)** counts as success. After a success the price resets to unknown and the next attempt waits until the balance passes the old price again (`retryPriceGrowthPct` margin). Unknown prices get one exploratory send per kind: **zone first while the TTK gate is open** (0.9.16), sword first while it is closed. `upgradeMinIntervalMs` (60s) per kind is a backstop ceiling, collapsed to `commandCooldownMs` while the balance is `cooldownRelaxBalanceMult` (3×) over the kind's last price (right after a rebirth the balance grows 10× a minute; logs show zone skipped for "cooldown" from 8M to 220M).

**Strict chat gate.** Upgrade lines classify only within `upgradeResponseWindowMs` (4s) of our own send, never on player/broadcast lines (`»` or `[rank]` prefix), and only via anchored patterns. This kills the 0.8.x failure mode where `EnchantedMC »` broadcasts matched the loose `enchanted` success pattern. Unrecognized lines are raw-logged (`upgrade_response_raw`) for 6s after each send, so reply formats are always captured for evidence-based tuning.

**Zone.** Same model for `/zone max` (fail gap → `zoneTarget = bal + gap`), behind the hard TTK gate below. A successful zone advance teleports you, so the stop protocol exempts displacements for `expectedTeleportAfterZoneMs` (8s) after our own zone send. EnchantedMC's sidebar has **no Zone row**, so zone changes come from our own teleport (`zone_teleport`), the mob level prefix on boss bars (`LVL2 Rabbit` → `LVL5 Goat`, logged as `boss_level`, debounced over two polls), the `All mobs have been respawned in your zone.` broadcast, or a `Zone N` row where a server has one. Any of them fires `zone_change`, clears the TTK window and forces a full targeting reset (`zone_retarget`).

**Rebirth reset.** A rebirth zeroes money and resets sword/zone progression, so every learned price is stale the moment it happens. Detected two ways: the sidebar `rebirth: N` counter incrementing (or resetting, for ascensions), and a money collapse (≥1B → ~0 while we didn't just buy something). Either wipes all learned prices, gaps, maxed flags, and seed flags (`economy_reset` event) — the loop re-discovers post-rebirth prices with one fresh seed send per kind.

**Sword vs zone: a TTK gate with rolled patience.** `/zone max` is refused — affordable or not — while the *effective TTK* is above this stage's **patience** or unknown. Since 0.9.23 the effective TTK is the rolling kill median (`ttkWindowKills`) as soon as three kills of the stage have landed — what actually happened — and the DPS-predicted whole-mob time (`HP at tag / boss-bar DPS`) only before that, and only while it is fresh (`predictedTtkMaxAgeMs`, 4s: a prediction describes the mob it was read from and is refreshed every tick while that mob is cooked; instant kills never produce one). Patience is not a fixed 10s line: every zone change and enable rolls it log-normally between `zoneMaxTtkMs × zonePatienceMinMult` (0.6) and `× zonePatienceMaxMult` (1.6), logged as `zone_patience`, so the same 8s median opens the gate one stage and closes it the next; `upgrade_plan`/`upgrade_skip` carry `ttkVia` (median/predicted) and `patienceMs`. The window is cleared on every zone change, so the previous stage's fast kills can never open the gate for the next stage. This is what stops the 0.9.5 spiral, where a readiness value pinned at 100% bought three zones against three swords and TTK went 0.25s → 3.8s → 7.2s → 75s → 90s until nothing died. With the gate open the **zone is the buy** (0.9.16): bought when affordable, probed first when its price is unknown, saved for otherwise. Rebirth, once its gap is known and covered, pre-empts both.

**Zone first, then sword, then rebirth (0.9.16).** The 03-36 post-rebirth log settles the order: every zone step multiplied per-kill money ×20 (58.7K → 1.17M → 23M → 462M → 9.2B → 183B) and cost about one kill of the next zone (137B for zone 6 whose kills pay 183B), so it pays back on the first kill even at a 42s TTK; a sword tier costs ×3.5 the last and halves the TTK only until the TTK reaches the movement floor (0.7–2s), after which it does nothing (zone 1: 675K spent on swords at 0.73s next to a 145K zone; zone 3: 376M before a ~250M zone). The priority is therefore: rebirth when covered; **sword while the gate is closed** (TTK above `zoneMaxTtkMs`, the only thing that helps and the guard against the zone spiral); **zone while it is open** — and while saving for a known zone price a sword is bought only when it costs at most `swordWhileSavingMaxPct` (25%) of the remaining gap and the TTK is above `zoneInstantTtkMs` (2s). Held swords log `upgrade_skip reason=saving-zone` (`zoneGap`, `swordPct`, `ttkMs`); `upgrade_plan` carries `priority`; the HUD plan row says `save for zone …`. (0.9.33 reworked the gate itself — see the 0.9.33 paragraph.) `status` events carry `zoneReady` (1.0 with the gate open, `zoneMaxTtkMs / ttk` above it).

**Rebirth horizon (0.9.15).** A sword or zone bought before a rebirth is lost with it, so it has to pay for itself first. With income I per minute, price P, remaining gap G to the rebirth and an income multiplier g expected from the buy, staying rebirths in G/I minutes and buying in (G+P)/(I·g); the bot buys only when that is sooner, i.e. **P < G·(g−1)** (`rebirthHorizonZoneGain` 1.3, `rebirthHorizonSwordGain` 1.25, `rebirthHorizonEnabled`). Evidence from 2026-09-03: with the 900T rebirth 475T away at 109T/min (4.4 min), the bot bought zone 8 for 415T; income rose to ~148T/min but the rebirth was still 314T away 8 min later. In an earlier session zone 7 for 7.55T against a 22T gap turned a 9-min rebirth into a 14-min one. Early-snowball buys (K–B prices against a T gap) pass untouched. Blocked buys log `upgrade_skip reason=rebirth-horizon` with `rebirthEtaMin`, `buyEtaMin`, `gain`, `gapPct`; the HUD shows `eta 4m` next to the rebirth target and `(saving; zone waits)`. Every sword/zone success is followed `rebirthHorizonGainWindowMs` (3 min) later by an `upgrade_gain` line (income before/after, ratio, kills) so the gain knobs can be tuned from evidence; it is not used for decisions.

**Cook timeout.** `maxCookMs` (90s) abandons a tagged mob only once its boss-bar HP has also stopped dropping for `cookStallMs` (15s). Fresh-stage mobs can legitimately take that long (the Goat in the logs died at 89.8s; 0.9.6 abandoned the next one at 90.0s, one second before it died, and lost the kill credit and its 6.48B).

**0.9.25: the suffix ladder heals itself.** QQ was the third suffix this server taught us the hard way (Q in 0.9.6, Qa guessed wrong in 0.9.19, QQ in 0.9.24), and the rebirth cost grows ~30× per rebirth, so a new one arrives every second or third rebirth. The money row is read every second, and the one moment a new suffix's scale is provable is the rung crossing: `903.74T` one poll, `1.1Q` the next. So the built-in table is now only what the server has printed (K M B T Q QQ), and every change of the money row's suffix is judged by one rule (`Amounts.crossing`): the new suffix is the next 1000× rung iff its mantissa is in [1, 1000), the previous poll is at most 5 s old, and `mantissa × scale(previous) × 1000 / previous value` lies in [0.95, `suffixCrossingMaxJump` (20)] — adjacent rungs differ by 1000× while the band spans ~20×, so a spend, a rebirth collapse or a two-rung skip lands out of band. Every rung change logs `suffix_crossing` (from, to, verdict, reason, ratio), on the known rungs too, so the rule proves itself on the live board once per rebirth cycle. A fit on a suffix the table lacks is `suffix_learned` (confirmed when its basis was) and persisted with provenance in `config/ycbotchallenge-suffixes.json` (delete it to relearn). A suffix a chat line names before the board showed it (`$20.5QQQ` at a 2.66Q balance) or a board that is already on an unseen rung at enable gets the rung above the highest known scale, marked provisional (`suffix_guess`, `upgrade_chat provisional=true`, `balance provisional=true`); the balance must climb through that rung before it can afford anything quoted in it, and that crossing is `suffix_confirmed` or `suffix_corrected` — a correction forgets every provisional rung built on the wrong one and drops learned prices expressed on it (`dropped`, relearned from the next fail line). Config `suffixScales` win over learned scales and are never touched; `suffixLearningEnabled` false restores the 0.9.24 behavior. Above the top known rung, values print as a growing mantissa (`2500QQ`) rather than a guessed label.

**0.9.31: the price ladders.** Every log agrees on the formula Drew asked about: a sword level costs ×3.5 the last (36 consecutive steps from 150.06K to 472.7S, every ratio 3.49–3.50) and a zone stage ×55 the last (137.26B → 7.55T → 415.21T → 22.83Q → 1.26QQ → 69.08QQ → 3.8S → 208.97S; the 65.4QQ/67.35QQ readings of the same stage were fail lines against a one-second-old balance). So after a purchase the next price is `last × growth` (`swordPriceGrowth` 3.5, `zonePriceGrowth` 55) and becomes the kind's target at once, marked predicted (`price_predicted`, HUD `predicted`, `upgrade_plan`/`upgrade_skip` `swordVia`/`zoneVia` = server|predicted) — no exploratory send is needed to learn it, and the horizon and saving-zone rules have their numbers immediately. The next server line checks it (`price_check` with `errPct`; a purchase at a predicted price logs `price_predicted_ok`), and a measured ratio between two consecutive server prices within `priceGrowthLearnBandPct` (30 %) of the config value is blended into the per-account growth (`price_ratio`, persisted in the state file; a `/zone max` that bought two stages lands outside the band and is only logged). `pricePredictionEnabled` turns it off; a rebirth wipes the predictions with the prices.

**0.9.30: the sword gain follows the TTK.** The horizon rule assumed every sword tier lifts income 1.25× (`rebirthHorizonSwordGain`); the logs of 2026-09-03 measured the same-zone kill medians around 45 sword buys and found 1.3× at the movement floor and 2–8× on long kills (22.3 s → 2.7 s on lvl8), and `upgrade_gain` ratios of 3.1, 4.2, 18 and 4.3. At the fixed 1.25 the rule refused a 132S sword at a 156S balance against a 656S rebirth with 5–8 s kills — a buy that would have cut the remaining ~29 min to ~11. Now the sword gain is computed from the current effective TTK (`Economy.swordGain`): a tier multiplies DPS by `rebirthHorizonSwordDpsMult` (2.0), income goes as 1/(ttk + `zoneInstantTtkMs`), so the gain is (ttk + floor)/(max(floor, ttk/mult) + floor), never below `rebirthHorizonSwordGain` (1.25, now the floor). With floor 2 s: 1 s → 1.25, 5 s → 1.56, 8 s → 1.67, 20 s → 1.83, 60 s → 1.94 — conservative against every measured pair. `upgrade_skip`/`upgrade_plan` log the `gain` actually used. The zone gain stays the 1.3 constant (one sample, 1.55).

**0.9.33: zone-first for real — the gate has three states and "unknown" is not "closed".** The 2026-09-04 audit (logs 03-48, 05-39, 05-55; rebirths 9 → 10) found the buy loop inverted right where the snowball lives: every `/zone max` teleport empties the kill window, the 0.9.23 gate read an unknown TTK as closed, and `chooseBuyKind` fell through to "sword" — so **11 of 12 zone buys were followed by a blind sword buy within three minutes**: 15.98Q for a sword on lvl7 with 17.33Q in hand and a 4.4T zone floor (~3,600× cheaper, ×600 per-kill income; that sword moved the TTK 1251 → 1200 ms), 2.48T four seconds before the median opened the gate, and with `zoneTarget` unknown the saving guard was skipped entirely. A slow RARE mob's DPS prediction also inflated the horizon gain: at 05:56:13 a 5.79SS sword went out 12.3SS short of the 19.68SS rebirth (`ttkVia=predicted 35447`; the median three seconds later was 5.5 s — fourteen minutes earlier with a median the same state correctly logged `saving-zone`). Now every eval is one `Decision` (`Economy.decide`): the zone gate is **OPEN** (the stage's kill median under the rolled patience), **HARD** (median above it, one of the first two kills above it, or the mob being cooked has *already* taken longer than the patience — rarity-normalised elapsed time, real evidence, never the prediction; `zone_gate_hard via=cook`), or **UNKNOWN** (nothing measured yet). The zone is the buy whenever the stage is not HARD — probed first while its price is unknown, after `zoneMinStageKills` (1) kills on the stage the last `/zone max` landed on — and the sword only when the stage is HARD, or while it is cheap against the zone gap (`swordWhileSavingMaxPct`, with the gap estimated from the zone floor × ladder when the target is unknown instead of bypassing the guard) and the kills sit above `zoneInstantTtkMs`; the horizon sword gain comes from the median only (unknown → the 1.25 floor). The overshoot Drew described — a post-rebirth `/zone max` landing on a stage the sword cannot handle — reads HARD during the first slow fight, so the sword buys start before that kill even ends, and the zone is the buy again once three kills sit under the patience. The first target on a fresh stage is a common mob (`stageProbeCommonKills` 1, `stageProbeRarityPenaltyBlocks` 30; `target_common_first`, `next_picked commonFirst`) so the stage is measured quickly; a bot toggle within `ttkKeepOnReenableMs` (60 s) on the same stage keeps the window and the first-kills roll (six toggles in 37 s had emptied it). `upgrade_plan`/`upgrade_skip` carry `decision`, `reason` (`zone-affordable`, `zone-probe`, `zone-stage-kills`, `saving-zone`, `sword-cheap`, `sword-hard`, `sword-instant`, `rebirth-horizon`, `unaffordable`, `first-kills`, `cooldown`, `hesitate`, …), `gate`, `gateVia`, `stageKills`, `zoneGapVia`, `gainVia` and the log-only `predictedMs`; `zoneGate` keeps open/closed (closed = HARD) for the analyzers. `gateUsesPrediction` restores the old prediction-filled gate. The HUD plan row is the same object (below).

**0.9.29: nothing typed before the first kill.** `upgrade_plan kind=sword` fired 5 s after each enable with zero kills (00:19 log; the kill counter carries over a toggle, so the first eval read as a kill) and `via=timer` 32 s into a session before any kill (17:57). Now no `/swordmax` or `/zone max` goes out before a rolled `upgradeFirstKillsMin..Max` (1–3) kills since the enable *and* since the last rebirth (`upgrade_skip reason=first-kills`); the phantom first eval is gone.

**0.9.24: QQ, and a /rebirth probe that never loops.** The 18:37 log: the stale-floor probe opened `/rebirth`, clicked the diamond, and the server answered `You need $20.xQQ Money to Rebirth.` — a suffix the parser did not know (`latest.log`: `Unknown amount suffix 'QQ'`), so the line was held as the first half of a split fail, nothing was recorded, the 2s settle timed out, and the abort path re-typed `/rebirth` 8–20s later — five times in 80s — until a closed GUI was read as a rebirth (`upgrade_result kind=rebirth via=silence`, `economy_reset via=upgrade-success` with the balance still 2.66Q). Now: `QQ` = 1e18 is built in; a need line with a suffix the parser still cannot scale logs `amount_unknown` (kind, token, suffix, raw) and resolves the send as a fail (`upgrade_result via=unknown-amount`) instead of a timeout; a rebirth is confirmed only by the server's own signals — its chat line, the sidebar counter, the money collapse — inside `rebirthSignalWaitMs` (6s) after the click, never by a GUI that merely closed (`upgrade_abort reason=no-signal`, no economy reset); and an aborted probe is re-typed only when it never reached the GUI (`no-gui`/`no-diamond`), at most `rebirthProbeMaxRetries` (1) per session (`rebirth_probe_retry` / `rebirth_probe_unresolved`).

**0.9.23: the gate reads real kills, patience is a mood, stale floors get probed.** The 17:57 log (rebirth 4 → 5 mid-session) showed the sword bias after a rebirth: the first chicken on zone 1 took 11.5s with the level-0 sword, its DPS prediction went into `lastPredictedTtkMs`, and because the prediction outranked the median it sat in every eval for the next two minutes (`"ttkMs":11502,"zoneGate":"closed"` × 34) while `zone_benchmark` medians fell 1206 → 301 ms — one-click chickens never refresh a prediction. Result: 2.57M + 6.43M + 22.52M of swords, zero zone probes, on a stage where the zone is the buy (the 17:31 run, enabled fresh on zone 1 with no stale prediction, bought five zones in four minutes). Three changes, one idea — decide from what is actually happening, with a human's tolerance rather than a line: (1) median first, prediction only before three kills and only while fresh; (2) patience rolled per stage (`zone_patience`); (3) the persisted rebirth floor is a lower bound from an earlier rebirth — once the balance sits on it with no rebirth having fired it is stale (155Q against a 900T floor from rebirth 3; the ~800Q cost of rebirth 5 was never learned, the horizon rule had no target, and a 489Q sword went out two minutes before the auto-rebirth), so the one-per-session `/rebirth` seed probe is due (`upgrade_plan kind=rebirth via=stale-floor`) and the horizon rule gets its target. No phase machine for early-vs-late in the rebirth cycle: kill speed (patience) covers the early snowball and the horizon rule covers the approach, both continuous; what had broken was their inputs.

The sidebar is reread every second: the money row feeds the buy path directly, other currencies (`sidebarCurrencies`) are logged, and snapshots publish every `scoreboardSnapshotMs` (5s). `debugSidebar` defaults to **true** while parsers are tuned from live evidence — every new non-currency sidebar row lands in the JSONL as `sidebar_raw` (0.9.33: currency rows are `balance` events only; 80% of a 38 MB bot-off log had been sidebar/balance rows). Every event row carries `bot` = `on` / `off` / `paused:<reason>` since 0.9.33, and while the bot is off `balance`/`income` rows are throttled to `offBotLogIntervalMs` (30 s) per currency unless the suffix changes or the balance halves. Upgrade responses that were not answers to our own send — Drew's manual `/swordmax`, `/zone max`, `/rebirth` — are logged as `upgrade_observed` and learned like ours (`learnObservedUpgrades`). A `/zone max` success names no amount, so its price is the sidebar drop right after it: `upgrade_paid via=sidebar-delta` (retry floor + ladder seed, never growth learning: `price_ratio deltaBased`), `upgrade_result paidPending`, `upgrade_paid_missed` when no drop lands. Money values in the JSONL are suffixed strings (`75.1B`), not scientific notation. The suffix order is the server's, K M B T **Q QQ** … (`1.25Q` = 1e15 in the fail lines; the server's quintillion is written `QQ`, seen 2026-09-03 18:43 as `You need $20.xQQ Money to Rebirth.` after a 2.66Q balance — 0.9.24). Nothing above QQ is built in: the ladder heals itself (0.9.25, below). The first sighting of each suffix on the sidebar logs `amount_suffix` with the row before it and its `confidence` (builtin/learned/provisional/config); a 99% money drop that does not land below `moneyCollapseMaxValue` (1T) is logged `suffix_scale_suspect` instead of being taken for a rebirth, and a provisional suffix never triggers the money-collapse reset. Manual overrides go under `suffixScales`. Config knobs auto-migrate (`configVersion` 11 forces the 0.9.7 defaults: success patterns, zone gate, eval triggers, cooldown relaxation, stall-aware cook timeout, `minKillsAfterAffordable` 0).

### 0.9.10: de-fingerprinting the loop

A timing pass over every 2026-09-02/03 log found server-visible regularities; each has a fix and a knob:

- **No success follow-up.** 0.9.x re-sent the same command 3.5–4.9s after every success to learn the next price (8/8, spread 10%). Now the next tier stays unknown until the balance passes the last paid price × (1 + a growth rolled per success in `retryPriceGrowthMinPct/MaxPct`, 0.2–0.8), logged as `retryAt` on `upgrade_result`.
- **No enable ritual.** Learned prices persist per **username** in `config/ycbotchallenge-state.json` (`state_loaded` / `state_saved`; wiped to the rebirth floor on a rebirth), so a restart never probes. An unknown account gets one `/rebirth` seed per session only after `rebirthSeedMinKillsMin/Max` (5–20) kills **and** `rebirthSeedDelayMin/MaxMs` (2–10 min). After a rebirth the old cost is a floor: one deferred re-probe learns the next goal after `rebirthReprobeMinKillsMin/Max` (15–40) kills and `rebirthReprobeDelayMin/MaxMs` (5–15 min) (`upgrade_plan via=reprobe`), or earlier if money passes the floor × (1 + roll ≤ `rebirthRetryFloorGrowthMaxPct`) (`via=retry-floor`).
- **Rebirth settle.** After our own teleport the bot stands `postRebirthSettleMin/MaxMs` (4–9s; zone advances `postZoneSettleMin/MaxMs` 2–5s), glances around (`postTeleportLookChance`, `mouse_flick reason=settle-look`), and only then retargets (`settle_start` / `settle_end`). 0.9.9 tagged a stale mob one tick after the rebirth teleport.
- **Buy hesitation on long saves.** With `buyHesitationChance` (0.3) an affordable buy is held `buyHesitationMin/MaxMs` (30s–3min) — only when the price has been known for `buyHesitationMinSaveMs` (2 min) and the balance is under `cooldownRelaxBalanceMult` × price. Never in the post-rebirth snowball (back to zone 1, `/swordmax` levels lost, enchants kept, balance 10×/min), never for rebirth, never twice in a row (`upgrade_hesitate`, `upgrade_skip reason=hesitate`). `buyNoticeDelayMaxMs` is 15s.
- **Reaction floor** `reactionDelayMinMs` 200 (was 120). **Bimodal breaks:** `breakShortChance` (0.7) of `breakShortMin/MaxMs` (20–60s), else `breakLongMinutesMin/Max` (5–15 min); `break_start kind=short|long`.
- **Aim and movement.** The re-aim threshold scales with distance (`reacquireFarMult` ×3 at `reacquireFarBlocks` beyond reach, base inside `reacquireFinalBlocks`) — no more 300ms correction chains while walking. Turns over `bigTurnDeg` (60°) land `bigTurnShortMin/MaxPct` (8–15%) short and get a settle flick (`mouse_flick short=true` then `settle=true`); `flickMaxDurationMs` 1100 replaces the 700ms wall. Per-target reach is jittered (`reachJitterPct` 0.2) with an occasional overshoot (`overshootChance`). Long cooks get glances (`cookGlanceAfterMs`, every `cookGlanceMin/MaxMs`; `reason=glance` / `glance-back`).

### Enchants during long kills

The server auto-attacks a tagged mob until it dies, so a fresh-stage 50–120s kill is idle time. During a long cook (ETA at least `enchantCookMinEtaMs`) or in a lull between kills, the bot right-clicks with the sword to open the **SWORD ENCHANTER**, walks the `enchantTabs` (souls → essence → shards, each spending its own sidebar currency), and for the **first** enchant in slot order that is not `LOCKED`, not maxed, and affordable, opens its `<Name> Upgrade` GUI and clicks **Max Upgrade** (buys level 1 upward for an unowned enchant). Then the next enchant, then the next tab. No optimisation by design.

0.9.19: the enchanter's slots arrive a tick or two after its screen, so an unrecognised container right after the right-click is "not yet", not "wrong", until `enchantOpenTimeoutMs` (the 16:31 log aborted `wrong-gui` 0.2s in and left the menu open); and any menu of ours that nobody is driving is closed after `strayGuiCloseMs` (8s, `stray_gui_close`) instead of idling the bot until the toggle key.

Evidence-based details: the enchanter's title is formatting-only (a font glyph draws the words), so the GUI is recognised by its item tooltips (`enchantSignaturePattern`, "ACTIVATION CHANCE") — and a hand-opened enchanter is therefore never mistaken for a captcha (0.9.8 paused on every manual open). Enchant state comes from the tooltips: `Level: 1,321 / 2,000`, `Price: 7,105,000 Souls`, `LOCKED (Requires Sword Level 50)`; the Max Upgrade hopper's `* Levels: 1` / `* Price: …` decides the final click. The sword's own tooltip (only owned enchants) is logged once per visit as `sword_lore`, never used for decisions.

**When it opens (0.9.11 hazard).** The 0.9.9 "45s mob and 3 minutes" gate only ever opened the menu while a fresh-stage mob was being fought, so visits clustered right after zone advances — a fingerprint. Now every post-kill lull (`via=lull`) and, once per long cook with `enchantCookMinEtaMs` (20s) left (`via=cook`), rolls against a hazard: zero until `enchantHazardRampStartMs` (2 min) since the last visit, rising squared to `enchantHazardFullChance` (8%) at `enchantHazardRampFullMs` (12 min), multiplied by the affordability pull (balance over the cheapest upgradable price seen on the last scan, up to `enchantHazardPullMaxMult` 3×) and by `enchantHazardCookBonus` (2×) mid-cook. With ~5s kills the mean spacing is about 6 minutes with a long tail. No rolls for `enchantPostZoneQuietMin/MaxMs` (30–90s) after a zone advance. Growth since the last visit (`enchantMinBalanceGrowthPct`, 10%) is a soft condition: `enchantCuriosityChance` (10%) of visits happen anyway, scan, and close. `enchant_visit_start` logs `via`, `hazard`, `pull`, `curiosity`.

**What it buys (0.9.12).** No purchase caps: every visit walks all three tabs and Max-Upgrades every affordable enchant on each, at the same human pace per click (`enchantMaxMenuMs` 180s is only a safety cap). The enchanter opens on whatever tab you used last, so the controller first reads which tab is showing from the items' price currency (`enchant_tab_showing`) and only clicks a tab button when it is not already selected — a person does not click the selected tab. Tab buttons are recognised in the top row only, by the tab word or its singular ("SOULS", "Soul Enchants", "ꜱʜᴀʀᴅꜱ"); the raw names of every non-enchant item are logged once per open as `menuItems` for tuning. Affordability always uses the item's own price currency, never the tab's (0.9.11 bought Rocket for 29M essence while checking the souls balance). Only mid-cook visits end when the mob dies; a between-kills visit runs to completion. Every step waits a humanized pause (`enchantLook…`, `enchantTabSettle…`, `enchantBuySettle…`). If the mob is about to die (`enchantWrapUpEtaMs`) the purchase in flight completes and the menu closes; `enchantMaxMenuMs` (180s) caps a visit. Events: `enchant_visit_start`, `enchant_menu_open`, `enchant_tab`, `enchant_scan` (the grid with levels/prices), `enchant_pick`, `enchant_upgrade`, `enchant_skip` (cadence / rolled-skip / no-growth / no-sword / none-affordable / max-unaffordable / tab-missing), `enchant_wrap_up`, `enchant_reopen`, `enchant_abort`, `enchant_menu_close`. `enchantsEnabled: false` turns it off; `enchantOpenViaInteract` swaps the synthetic use-key press for a direct interact call if a server build ignores the key.

**0.9.30: a roll, not the first slot.** Ten essence visits in a row put the whole balance into Rocket (slot 11: 81, 29, 17, 55, 70, 24, 83, 126, 32, 166 levels) while Second Hand got the change and Wizard on the shards tab — third in slot order behind Blacksmith and Flamewright — was never bought once. The pick is now a weighted roll over every affordable non-maxed enchant on the tab (`EnchantLore.chooseEnchant` with a roll): weight `1 + enchantLagBias × (1 − level/maxLevel)`, so with `enchantLagBias` 0 (default, Drew's "equal chance per non-maxed enchant") each candidate is equally likely and 1–2 tilts toward the one furthest from max. `enchant_pick` logs `candidates`, `roll` and `lagBias`. Max Upgrade still buys every level it can, so the first pick of a visit still takes most of the balance — it is just a different enchant each time.

**0.9.30: a glance up before the right-click.** 7 of 22 visits aborted `no-gui` at `open_wait`, every one started the tick a kill landed: the dying mob was still under the crosshair for its death animation, so the use-key interacted with it instead of the sword (three in a row would have suspended enchants for the session). An entity under the crosshair now gets a look up by `enchantOpenClearPitchDeg` (15°) first, then the press; `enchant_open` logs `target` (entity/block/none) and `cleared` either way.

### Giveaways (0.9.17)

The server announces `NEW GIVEAWAY (30s to enter)` / prize / `Click to Enter!` and counts down; typing `/giveaway` joins (`You have joined the giveaway for Current Lootbox!`). With `giveawaysEnabled` (default on) each announcement rolls `giveawayJoinChance` (0.85), waits a log-normal reading delay (`giveawayJoinDelayMin/MaxMs`, 2.5–12s) and types the command through the same stop-and-type path as every other send; if it cannot go out within `giveawayWindowMs` (25s) it is dropped. Events: `giveaway_seen` (prize, raw packet), `giveaway_plan`, `giveaway_join` (delay, typos), `giveaway_joined`, `giveaway_result` (`us` when our name won), `giveaway_skip` (`disabled`, `chance`, `window`). Joining every giveaway within seconds is its own fingerprint, hence the toggle and the roll. When our own name wins (`<name> has won the giveaway for`, logged `giveaway_result us=true`) the bot says something 1.5–6s later, one of `giveawayWinMessages` (`lfg`, `gg`, `LFG`, `ggs`, `lets gooo`, `W`, ...) picked at random with `giveawayWinReplyChance` (0.9), typed like any line (`giveaway_reply`); `giveawayWinReplyEnabled` turns it off.

### Rebirth upgrades (0.9.17)

`/autorebirth` is unlocked (`serverAutoRebirth`, default on): the server rebirths the moment the cost is covered, so the bot never types `/rebirth` to rebirth (it still probes the GUI to learn the cost for the horizon rule). What each rebirth leaves behind is **rebirth points**, spent in `/rebirth` → the nether star (`REBIRTH UPGRADES`, lore `| Current Points: N`) → the `Upgrades` menu. Drew's order (`rebirthUpgradeOrder`): enchant proc (enchanted book), damage (red dye), essence (magma cream), souls (purple dye), each maxed before the next. A visit runs `rebirthUpgradeDelayMin/MaxMs` (20s–2min) after the post-rebirth settle, or once per session after a rolled kill count for leftover points, always between fights: typed `/rebirth`, a look, read the points (`rebirth_points`; zero = close), click the star, scan the menu (`rebirth_upgrade_menu` logs every tooltip the first time — the item tooltips are **not captured yet**, so the level/cost/maxed patterns `rebirthUpgradeLevel/Cost/MaxedPattern` are best guesses to tune from that log), pick by order (`rebirth_upgrade_pick`), click, settle, re-read: a changed tooltip is a purchase (`rebirth_upgrade_bought`, up to `rebirthUpgradeMaxClicks`), an unchanged one stops the visit (`rebirth_upgrade_stop`). A sub-menu opening from the click is logged (`rebirth_upgrade_submenu`) and closed, never clicked into blind; a revisit follows in 20–90s so the outcome is observed. 0.9.30: the once-per-enable leftover-points check typed `/rebirth` twelve times across two logs and read zero points every time with the rebirth counter unchanged between them — points only come with a rebirth. A zero-points read now remembers the rebirth count (`pointsCheckedAtRebirths`, persisted per account in the state file) and the enable check is skipped until the counter moves (`rebirth_upgrade_skip reason=checked-this-rebirth`); the post-rebirth visit is untouched.

### Companions (0.9.28)

Companions multiply money (`Multiplier: 156.38x Money` on the equipped Cow). The bot buys them the way Drew does, on its own: it finds the zone's **Companion Egg** by its hologram (`<Zone> Companion Egg / … / | Price: $… Money` — the zone prefix is never matched, the `Credit Egg` is never touched; `companionEggPattern`, `companionEggExcludePattern`), walks over (`companionEggReach`, `companionWalkTimeoutMs`, `companion_walk`), aims at the pedestal block under the lowest hologram line (`companionEggAimDrop`, pitch swept until the crosshair hits within `companionEggHitRadius`, `companion_aim` with the block id) and right-clicks. In the **Companion Eggs** GUI it clicks `OPEN: [Nx COMPANION EGG]` items (`companionOpenPattern`): the largest that fits the eggs still wanted (`companionEggsMin..Max` rolled per visit), at most `companionMaxIncomeMinutes` of income and `companionMaxBalancePct` of the balance; every open is confirmed by the sidebar money dropping and the delta becomes the per-egg price (the hologram's `$121.3300` format is unverified, so the first open is the probe). Then `/companion`: **Equip Best** (`companionEquipBestPattern`), the four equip slots (`companionEquipSlots`) logged before and after (`companion_equip`), a look at **Fuse Companions** (`companion_gui which=fuse`, nothing clicked yet — fusing is not automated until the layout is seen), and the **sliding-window delete**: keep the newest `companionKeepZones` (2: the current and the previous zone), `/companion bulkdelete ZONE STAGE` for older pairs never held by an equipped companion, at most `companionMaxBulkDeletes` per visit (`companion_delete_plan`, `companion_bulk_delete`). Every GUI is dumped verbatim as `companion_gui` (the fixture net for the next version).

**Finding the egg (0.9.29).** The egg is a real `minecraft:dragon_egg` block on a pedestal, so the bot locates it by scanning the blocks around the player (`companionEggScanRadius` 64 × `companionEggScanVertical` 12, once per 30 s while idle and once per visit; `companion_egg_scan` lists every egg found) and reads the hologram within `companionEggHologramReach` of each to tell the Companion egg from the Credit egg next to it. **Ctrl + Shift + toggle while looking at the egg, bot on or off,** saves it for the current stage (`config/ycbotchallenge-eggs.json`, `companion_egg_spotlight`); with the bot on it also queues the visit. Order of trust per visit: the spotlighted block, the block scan, the saved point for the stage, the hologram alone; when all fail, `companion_plates` dumps the nearest plates so the next fix comes from evidence.

**0.9.31: the egg gets clicked.** Two automatic visits walked to the egg and right-clicked an armor stand, then the pedestal stairs (`companion_aim hit=entity:Armor Stand`, `minecraft:polished_blackstone_stairs`, then `no-egg-gui`): the block scan had found both eggs but paired neither with a hologram — its window took plate lines only from half a block under the egg's centre to 4.5 over it, and the Western plate group hangs a block lower — so the visit fell back to the hologram's guessed pedestal point, and the aim accepted any surface within 1.8 blocks of it. Now the pairing window is `companionEggHologramBelow` (3) under to `companionEggHologramAbove` (5) over the egg; with no pairing at all the egg block nearest the money hologram is taken (`eggVia=block-near-hologram`); the aim point is the egg's centre a quarter block up; and the right-click goes out only with the crosshair on a `minecraft:dragon_egg` block (a spotlighted entity keeps the distance test), sweeping pitch and yaw over ten tries and logging every miss (`companion_aim_miss`). **The saved egg is per location, not per stage** (`config/ycbotchallenge-eggs.json` keys `loc1`, `loc2`, …: Farm = stages 1–10, Western = 11–20, `companionStagesPerLocation`); old `lvl12` entries fold into their location on load, and every remembered egg point is forgotten on a rebirth or a location change (`companion_egg_forget`). **The 250× line taught the ladder SS.** The Companion Eggs GUI prices 1×, 3×, 10×, 50× and 250× openings; the 250× line read `1.58SS` while the 1× line read `6.34S`, a suffix the ladder had not seen, and a mixed `double`/`Double` expression unboxed the null price and crashed the client twice. The option is now skipped when unparsed (`companion_option_unparsed`), and the count ratio is a lesson: 250 × 6.34S = 1585S = 1.58SS, so SS is the 1000× rung above S — learned confirmed through the same path as a sidebar crossing (`companion_rung_lesson`, `suffix_learned via=gui-ratio`) and the GUI is re-read with all five prices parsed.

**When.** A sliding window on price vs income, not the top stage (Drew: a batch there costs ~1/4 of the rebirth). The hologram shows the single-egg price in every zone, so the bot logs `companion_price` (stage, price, minutes of income) as it passes. Once `companionStageSettleKills` kills on the stage have made the income figure its own, a visit is due when a batch of `companionEggsMin` eggs costs at most `companionMaxIncomeMinutes` (2.0) of income, the stage is `companionMinStageGain` (2) above the last purchase and the rebirth has fewer than `companionMaxVisitsPerRebirth` (2) visits; fallback when zone buys stop (horizon, zone maxed, or rebirth ETA ≤ `companionRebirthEtaMinMax`) with no visit this rebirth: one visit within `companionEndOfRebirthMaxIncomeMinutes` (8.0). A `companionDelayMin/MaxMs` (10–60 s) pause precedes the walk; `companion_plan` / `companion_skip` carry the numbers. **Ctrl + Shift + toggle** runs a visit now. Aborts log `companion_abort` (phase); three in a row suspend the feature for the session. The walk happens while combat is paused, and combat is reset afterwards so the stop protocol never reads the distance as a teleport.

**0.9.33: persisted, and never ahead of a stage.** The 14:22 visit spent 3.31SS of 9.91SS on ten eggs with 1.58SS left to the next stage, the 05:45 one 2.32SS for an unchanged equip list, and every restart forgot the last bought stage, the visits this rebirth and where the egg was (only the manual spotlight wrote the egg file). Now the last bought stage, the visits this rebirth (with the rebirth they belong to), the once-per-rebirth end visit and the per-stage observed egg price live in `config/ycbotchallenge-state.json` per user (`state_loaded` shows them), an egg found by the block scan is saved too (`companion_egg_saved via=scan`), the persisted per-egg price replaces the hologram read for the batch estimate, and a cheap visit is refused while a zone is affordable or the eval's next action is a zone buy (`companion_skip reason=zone-affordable`) or when the batch exceeds `companionMaxZoneGapPct` (25%) of the remaining zone gap (`zone-gap`; `zoneGap`, `batchPctOfGap` on `companion_plan`/`companion_skip`). The end-of-rebirth visit is exempt: zone buys have stopped by then.

**0.9.35: the egg batch is priced by the one economy, and it is the buy when the ladder outruns income.** Zone prices climb a flat ×55 a stage, but income growth per stage fell to ×24 at lvl14 and ×18 at lvl15 in the 2026-09-04 logs — income is money/kill × kills/min and kills/min collapses as the TTK runs 1.2s → 5s → 12s → 36s → 96s, so the ×3.5 sword ladder cannot hold that line and the climb decelerates without bound (the 17:21 session: **lvl15 for 41.7 bot-on minutes**, gate HARD, zone ETA 85–5,545 min, thirteen sword buys, no advance). A companion batch is a direct income multiplier that ignores the TTK — measured **2.20×** (7 eggs, 2.32SS) and **1.76×** (8 eggs, 7.23O) against a sword `gain` of 1.25–1.93 and a zone gain of 1.3 — and unlike them **it survives the rebirth**, which is why Drew buys late and high. The old trigger refused all of it: in 33 logs there are **4 completed purchases**, blocked by `stage-gain` (two stages above the last buy — permanent on a stage you never leave), `visits` (both per-rebirth visits spent on lvl15), `price` (a batch under `companionMaxIncomeMinutes` of income, and income lags a fresh stage exactly when it is hardest) and `zone-affordable` (the zone *was* affordable, but gated HARD — the buy was never going to happen). That was a second, weaker economy reaching into the real one through three back-channels. Now `Economy.decide` prices the batch itself and publishes a `Decision` of kind `companion`, as a **post-pass that can only convert a hold into a buy** — a real zone/sword/rebirth buy always wins, so zone-first is untouched. The batch goes out when it reaches the rebirth sooner than standing still (`companion-sooner`), or, near a rebirth or once zone buys have stopped, when it delays the rebirth by at most `companionMaxRebirthDelayMin` (3 min) and `companionMaxRebirthDelayPct` (25 %) — because a companion keeps paying on the other side (`companion-persist`, `companion-end`). `rebirthHorizonAllows`' rule `P < G(g-1)` stays the veto for the sword and the zone, which the rebirth wipes; `companionHorizonAllows` is its persistent twin. Eggs only pre-empt while the **stage** is at least `companionPatienceMinutes` (20) away — the zone ETA only, since the sword's is nearly always short and a close rebirth is a reason to buy, not to refuse — and they must beat the zone on ETA and come within `companionPersistCredit` (1.25×) of the sword. The cap is **per stage** (`companionMaxVisitsPerStage`, 2) with `companionMaxVisitsPerRebirth` (8) left as a runaway backstop. The batch price is `companionEggsMin` eggs on the egg ladder, measured **×52.2 a stage** over seven consecutive steps (44.44Q@s9 → 904.27SS@s15, every ratio 52.21–52.30), so a stage never visited still has a price; the gain starts at `companionGainPrior` (1.5, under both measurements) and is replaced by what the income actually does across each visit (`companion_gain`, `companion_ratio`, persisted). `upgrade_plan`/`companion_plan` carry `decision`/`reason`/`gain`/`gainVia` in the same vocabulary as every other buy, and `CompanionController` is left with feasibility only (`companion_skip` reasons `busy`, `settle-kills`, `no-egg-price`, `abort-cooldown`, `visits`).

**Three parsing bugs found on the way (0.9.35).** `companionMultiplierPattern` could not match a suffixed multiplier — the lore reads `Multiplier: 1.02Kx Money` and `[\d,.]+` stopped at `1.02` — so every companion the account owns read as multiplier-less. `companionEquipSlots` was `{0,1,2,3}` while the real GUI equips slots **1, 2, 3 and 5** (4 is `Equip Best`, 6–7 are `Slot #N Locked`, 0 is empty), so the bot saw three of four equipped and filed the fourth as storage — which let the bulk delete plan a pair an equipped companion held; equipped is now read from the `Click Here to un-equip` lore, with the slot list as fallback. And `companionFusePattern` looked for "Fuse Companions" while the item is called **"Companions Fusion"**, so in 33 logs the fusion menu was never opened once and every visit logged a misleading `no-fuse-item`. **Fusing is still not automated** — the pattern fix only reaches the menu so its layout is dumped as `companion_gui which=fuse`, the fixture a later version needs. Worth knowing when it comes: the item says *"combine companions together to increase the Companions level multiplier; each companion provides a 20 % success rate (maximum of 5 = 100 %)"*, and there are 37–45 idle companions in storage against a 600 cap. The neighbouring `Companion Upgrades` item is priced in **Orbs**, a currency that appears nowhere in the sidebar or in any log, so it stays out of scope.

**0.9.36: eggs that actually get bought, and a record that says whether `/zone previous` would pay.** The 19:14 log of 2026-09-04 (the first on 0.9.35) bought **zero** batches and logged **zero** companion events: `decideCompanion` returned bare `null` on six paths. Replayed against every eval, the batch was refused 120× by the ten-kill settle (16+ minutes at a 185 s first kill), 28× by the 40 %-of-balance cap (`18.53N` in hand for a `7.41N` batch while the whole rebirth cost `15.94N` — unreachable by construction), 31× by the 20-minute stage patience, and 8× by `companionMaxRebirthDelayPct`: 25 % of a two-minute rebirth is 30 s, so the rule forbade the near-rebirth batch its own comment said to buy. Meanwhile the wall moved one stage up: income growth per stage was ×27.8 at lvl15 and ×45.6 at lvl16, then **×6.4 at lvl17** against the flat ×55 zone price (`105.17N`, `price_ratio` 55.06) — lvl1→14 in 9 minutes, lvl14→17 in 51, then 62 minutes on lvl17 until the server auto-rebirthed. The ETA race is gone. Two rules now (`Economy.decideCompanion`): **`companion-sooner`** — the batch reaches the gap being saved for (the next stage, or the rebirth) sooner with the eggs than without, `P < G(g−1)`, the same algebra as the rebirth horizon and income-free, so it works on a fresh hard stage where the 60 s summary reads ~0; **`companion-persist`** — otherwise, once the income figure is this stage's own (`companionStageSettleKills` 3 *or* `companionStageSettleMs` 2 min), the minutes the batch delays the rebirth fit the persistent payback budget `max(companionMaxRebirthDelayMin, (1 − 1/g) × cycle × companionPaybackFraction)`, where the cycle is the last rebirth's **bot-on minutes** (`cycle_end`, persisted; `companionCyclePriorMin` 45 before one is measured) — a companion survives the rebirth, so its delay is paid back on the other side, never by a share of what is left. Before either: the controller can run a visit, a price is known, the stage is **not below the last one bought** (`below-owned`; Drew buys late and high, and a lower stage's eggs are already owned), the per-stage cap (`companionMaxVisitsPerStage` 2, the second visit waiting out the first's `companion_gain` window), and the balance covers the batch — the 40 % cap now applies to manual visits only. **Every decline is named:** the hold keeps its own reason and HUD line and carries `eggs=blocked|no-price|below-owned|repeat|unaffordable|settle|no-income|no-rebirth-target|horizon` on every `upgrade_skip`/`upgrade_plan` row, a throttled `companion_skip` carries the numbers (`batch`, `batchPct`, `gapZone`, `gapRebirth`, `soonerZone`, `soonerRebirth`, `delayMin`, `budgetMin`, `cycleMin`), and the HUD `busy` row and the Y screen show `eggs 7.41N 52% · unaffordable`. `companionGainMin` is 0.8 so a dud batch can lower the learned gain. The retreat Drew described — `/zone previous`, farm swords, `/zone next` — is **measured, not sent**: every stage of a rebirth gets a `stage_record` on exit (bot-on minutes, kills, money earned, money per kill, average and peak income, kill median, sword buys), and on a fresh stage the first time the gate reads HARD, and after every sword buy on it, `zone_back_candidate` compares what this stage earns per minute right now (money per kill over the kill time — the stage's own kills once one landed, else the previous stage's × `zoneMoneyGrowthPrior` 20) with the previous stage's peak, and says `wouldRetreat` when there beats here by `zoneBackMargin` (1.5). The 19:14 log's answer at lvl16→17 was no: the ×27 money-per-kill step cancelled the ×29 kill-time step (fresh lvl17 earned ~300–450O/min through the 185–250 s first kills against lvl16's 356O/min peak). Two fixes on the way: the 0.9.35 `companionFusePattern` correction never reached a live config (the migration only filled a blank pattern), and `prestige`/`ascension` chat detection fired on other players' lines (`» how do i prestige`). Config v40; the 0.9.35 race knobs (`companionPatienceMinutes`, `companionPersistCredit`, `companionMaxRebirthDelayPct`, `companionRebirthEtaMinMax`) are gone.

**0.9.37: fuse first, eggs against the nearer milestone, a hand purchase learned, one record per stage.** The first 0.9.36 session (23:18, 47 bot-on min, rebirth 14) bought six eggs for `5.13N` (0.8 % of income) and the equipped set went `3.04K/3.04K/3.04K/3.67K` → `9.95K/9.95K/5.5K/3.67K` — the rules work — and showed four things. (1) Every visit opened the **fusion menu and never fused**: the dumps hold `Fuse All` at slot 53 ("fuse ALL 5 of the same Companions") and groups of 7 and 6 identical companions at 100 % odds. Now the companions page is read for groups of `companionFuseMinGroup` (5) identical companions (name, zone, stage, rarity: `companion_fuse_groups`); with one, the visit goes **fusion → `Fuse All` → dump whatever the server shows (`companion_gui which=fuse-after`, a confirmation is never clicked blind) → `companion_fuse` → `/companion` again → Equip Best**; without one it skips the menu (`companion_skip no-fuse-group`). `companion_visit_done` carries `fused` and `equipChanged`. (2) At 00:05:21 the bot planned a `387.18N` batch — 92 % of the balance, the `478.3N` rebirth 63 min away — because the stage-18 *zone* gap (~5,800N) made `P < G(g−1)` trivially true; the zone now counts only while it is the **nearer milestone** (`Economy.companionMilestone`, `companion_skip milestone=zone|rebirth`), otherwise only the rebirth gap can make a batch "sooner", and the payback rule decides the rest (Drew kept it: that batch, bought by hand, passes on a 10.5-min delay against a 10.6-min budget). (3) Visit 2 on lvl16 bought three eggs and changed nothing in the equipped set (1.17× measured): a bought visit that changes nothing **saturates the stage** (`companionSaturatedStage`, persisted; `eggs=saturated`) until a visit there moves the set or a higher stage is bought. (4) The hand purchase was invisible: `422.58N → 35.61N` with the bot off is exactly `3 × 128.99N`. A sidebar drop of 1, 3 or 10 eggs at the stage's price (`companionObservedTolerancePct` 3) with no visit running and no upgrade just sent is `companion_observed` and counts as a visit. Also: the live config still carried the pre-0.9.35 `companionMultiplierPattern` (the v39 migration only filled blanks, like v40's fusion pattern) so every companion read as multiplier-less through 0.9.36 — v41 replaces a pattern that still equals an old default (`replaceIfOld`), and `companion_equip` now logs `multBefore`/`multAfter`; `stage_record` no longer splits on a mob respawn (`Economy.stageRecordRolls`); the souls tab, 28/28 enchants MAX with `163.87T` idle, logs `enchant_skip all-maxed` once and is not clicked again that session; `stray_gui_close` names the screen's items.

**0.9.37: the climb.** Five rebirth→lvl14 climbs from 2026-09-04 (81 live minutes; 47 more were bot-off) put a third of the live time between a teleport and the first kill on the fresh stage: the sword is one tier short on every stage (first kills 27–107 s) and the bot only learned it at the **30 s fallback eval** — the eval ran on a kill, a money increase or the timer, and a long first cook produces neither of the first two (`zone_gate_hard via=cook elapsedMs 30600 patienceMs 7210`, 22 times, 641 s). Every climb then needed a second `/swordmax` 10–40 s after the first. Four `/zone max` fails on a stale ladder floor cost 71–131 s each under the 60 s per-kind cap although the fail line had just quoted the exact price; the 2–15 s buy notice cost 3 min; an enchant visit inside the lvl4 leg stole the chat (`upgrade_abort chat-closed`). Now: a cook arms an eval at **tag + patience** (`via=cook-patience`, 7–16 s instead of 30); on a stage with no kill yet a fresh DPS prediction over `zoneMaxTtkMs × stageProbePredictedMult` (3) reads HARD at once (`via=predicted-fresh` — first kill only, fresh only, large margin only, so the 0.9.23 stale-prediction bug cannot return); a sword bought mid-fight is **re-judged on the same fight** `upgradeSpendSettleMs` later (`via=sword-recheck`); a server-quoted price younger than `serverQuoteRelaxMs` (5 min) collapses the per-kind cap; while the balance is `cooldownRelaxBalanceMult`× the price the notice rolls from `buyNoticeSnowballMin/MaxMs` (0.5–3 s); no enchant visit starts with a buy decided or typing (`enchant_skip buy-pending`); the post-purchase `rebirth_upgrade` revisit is skipped when the points are already spent. The 9.5-minute best climb should approach five or six.

**0.9.37: the no-hit click spam.** `tag_intent → tag` converted 61 % across six logs, with 36 windows of 8 s+ clicking and no tag — the worst 72 s straight at a Horse (one flick, then 45 `misclick` rows with `aimErr` frozen at 53–67°, six re-picks of the same mob at exactly 12.000 s, ended by the toggle key) and a Goat that took 20 clicks over 31 s with the crosshair within 3° the whole time. Four causes, four fixes. **Aim latch:** a flick issued the tick a screen opened never completed and `isBusy()` stayed true for good (every long run starts right after a `stray_gui_close`) — a path older than its duration plus 500 ms now expires (`aim_path_expired`) and the stray close cancels the driver. **Approach spam:** the 2–3 cps closing-in swings fired inside reach and facing away — now only outside reach and within `approachClickMaxAimDeg` (25) of the mob. **No escape:** the hard-coded 12 s re-picked the same mob — now `noConnectTimeoutMs` (4 s) drops it (`target_abandoned reason=no-connect`), the next pick stands half a block closer, and after `noConnectIgnoreAfter` (2) runs the entity is ignored for the session. **Damage numbers as names:** the plate fallback took the first hologram line in the box, so `✧293.89QQ✧ Critical` and `+4.77T Money` became the target's name (100+ intents, 0 % conversion, and fake `kill via=death+money` credit) — a line that starts with a digit or a damage glyph is never a name. Evidence for the next step: the tap requires the crosshair raycast to be on the mob, and `no_ray {aimErr, hit, dist, reach}` says what the ray hit instead whenever the aim is in tolerance — that counter decides whether `reach` (3.0, vanilla's exact range) moves.

**0.9.37: gg like everyone else.** A store purchase prints a seven-line `████████ GG WAVE ACTIVATED!` block and a median 17 players answer `!GG!` 0.5–2.0 s later (p10/p50/p90 over 26 waves 0.50/1.30/2.00 s); a rare perk pull prints `EnchantedMC » NAME has just pulled Universal Perk 5 on their Sword!` (twice in the same millisecond) and gets free-form congratulations. The bot had never typed a free-text line. Now (`ggEnabled`, Y screen "GG replies"): a wave gets `gg` on `ggWaveChance` (0.85) of them after `ggWaveDelayMin/MaxMs` (0.3–1.5 s; the typing pipeline adds its own second or two — `gg_reply sinceMs` is the calibration), dropped past `ggWaveWindowMs` (6 s); a perk pull matching `ggPerkPatterns` (Universal Perk 5) gets a phrase from `ggPerkMessages` on `ggPerkChance` (0.5) after 2–8 s. Never twice within `ggMinGapMs` (60 s), never off a player's own line (the wave block carries no `»`, the perk pattern carries the server's prefix). Events: `gg_seen` (kind, who, what), `gg_plan`, `gg_reply`, `gg_skip` (`chance`, `gap`, `window`, `disabled`).

**0.9.37: keeping score.** Bot-on minutes per stage and per rebirth, over time. The stage records of the rebirth in progress (`cycleStages`) and the last 30 finished cycles (`cycleHistory`: `rebirths`, `onMin`, `wallMin`, `toLvl14OnMin`, `topStage`, `stages`) live in the state file, so a restart mid-cycle keeps what was measured; `cycle_end` logs the whole list; the HUD rebirth row shows `cycle 47.9m (last 60.3m, lvl17)` and the Y screen's rebirth line the last three cycles. `tools/progress.py` reads every log (`--stages` for the per-stage table, `--since` for a date) and prints one line per rebirth — bot-on and wall minutes, minutes to lvl14, top stage, zone and sword buys, eggs — from the raw events on any log and from the bot's own records where they exist. Bot-on minutes are the honest metric: a paused bot does not count, and the wall minutes next to them show the pause instead of hiding it.

### The zone boss (0.9.38)

About every 89 minutes (70–119 over 19 spawns) the server spawns a personal boss in your zone — `the boss in your zone has just spawned, you will have 5 minutes to kill it before it despawns` — a large block-shaped body with its own bar (`Rotten Boss 300`, no heart, one count per hit), a title overlay (`Hit the targets to kill the boss and recieve the rewards!` / `Targets Hit - N`) and a small target marker on the body that relocates about every ten hits. A kill pays a Tier-2 Totem Box, skin boxes, sword perk rolls (3×, sometimes 50×), weak 15-minute boosters and the odd pet box — no money. In 36 hours of logs the bot never hit it once: the bar sat at 300 for 277 s of grinding beside it (`boost_end durationMs 277001`, 2026-09-04 18:03:04) until Drew toggled off and killed it by hand in 99 s.

**Detection.** The event bar (a bar matching `bossEventBarPattern` with no heart) is excluded from the mob-level tracker and published as `boss_bar` on appearance and on every count change, `boss_bar_hold` every 10 s while the count is frozen. Titles were invisible before 0.9.38 (the chat event carries chat and the action bar only; `Targets Hit` appears in none of 37 logs): `InGameHudMixin` hands `setTitle`/`setSubtitle` to the tracker — `boss_title` with `targetsHit`. Chat: `boss_spawn`, `boss_despawn`, `boss_killed` (`who`, `us`), `boss_reward` (the `Boss Reward:` participation line, and every server line within 20 s of our kill).

**The fight (`BossEventController`).** It outranks every buy — first in the tick, ahead of the upgrade typing — and starts on the bar or the start title, after the current kill unless the boss has waited `bossEventStartGraceMs` (8 s). **SCAN** dumps every non-player entity within `bossScanRadius` (12) as `boss_scan` (type id, position, box, name, plate, rank) and picks the marker by what vanilla's attack can land on: an `interaction` entity first (the smallest box, nearest the body), an armor stand with no plate or a plate matching `bossTargetNamePattern` second; a display is a picture with no hitbox and is **never attacked** — a hittable entity within 1.5 blocks of it is used instead, else `boss_abort reason=marker-not-hittable` with the scan attached. The body is the nearest block/item display, else the largest box. **WALK** goes to a stand point outside the body facing the marker, `reach − 0.5` blocks out along the body-to-marker vector (top face or degenerate: from the player's own side), recomputed every tick so a moved marker re-plans the walk; a stall sidesteps, alternating sides. **AIM** sweeps offsets until the crosshair raycast is on the marker entity. **HIT** taps only while the raycast stays on it, at `bossClickCpsMin/Max` (2.5–3.5, Drew's pace) without waiting for the vanilla cooldown (`bossRespectVanillaCooldown` false: the server counts hits, and the sword cooldown alone would take 190 s of the 300 s window); the marker moving `bossMarkerMoveBlocks`, the count falling `bossMarkerMoveHits` since the target, or `bossNoProgressMs` without progress rescans; `bossMaxRescans` without progress, `bossWalkTimeoutMs` twice, two failed aim sweeps, a screen for 5 s or `bossEventMaxMs` abort (`boss_abort` with the crosshair state); the bar leaving or the kill line is `boss_done`. `boss_target`, `boss_walk`, `boss_aim`, `boss_hit` (every `bossHitLogEvery`), `boss_marker_moved`, `boss_no_progress`; three aborts in a row suspend it for the session. Every exit resets combat so the walk is never read as a teleport. Y screen: "Zone boss".

### Transcend ability (0.9.28)

Q with the sword in hand activates Transcend; the server prints `Your Transcend Ability has been activated (180s Cooldown)` and `… has ended`. The bot reads the cooldown from that line (`transcendCooldownPattern`, fallback `transcendCooldownMs`) and, once it has passed, rolls per kill a chance that ramps from 0 to `transcendFullChance` over `transcendRampMs` — so the press lands somewhere after the cooldown, never on the dot, and never in the first `transcendFirstDelayMin/MaxMs` after enabling. The press is one real drop-key edge with the sword held and no screen open (`transcend_press`); if the hand is empty half a second later the press dropped the sword: `transcend_drop_suspect` and an emergency stop so Drew can pick it up. **0.9.30: the server does it itself.** The 00:19 log shows 37 activations at exactly 190 s spacing over 2.3 hours, two of them with the bot off and Drew away, against one press of ours — the ability is auto-cast on this account. An activation with no press of ours in the last `transcendPressGraceMs` (2 s) is the server's (`transcend_active ours=false`); `transcendAutoDetectCount` (2) of those in a row and the bot stops pressing for the session (`transcend_auto`, HUD chip `transc auto`), removing the sword-drop risk for nothing. A reconnect resets the detection.

### Options screen (0.9.30)

**Y** (rebind in Controls → Misc) opens a vanilla screen with one ON/OFF button per feature: server auto-rebirth (`serverAutoRebirth` — off on an account without `/autorebirth`, in which case the bot's own `/rebirth` buy path is used: typed command + diamond click, unexercised since 0.9.17, so watch `upgrade_send kind=rebirth` and `rebirth_confirmed` the first time), sword/zone buys, the rebirth horizon rule, enchant visits, rebirth upgrades, companions, companion bulk delete, Transcend, giveaways, the giveaway win reply, breaks, the stop protocol, captcha auto-solve, learning manual buys, Sword Skins scouting, the legacy prediction gate, the price ladder, sprint, the HUD and its plan / module / balances rows. Since 0.9.33 each button carries a grey status line: the plan line under sword/zone buys, what each visiting module is doing (or `suspended` / `idle`), the transcend state, the break countdown, the rebirth target, the sword tier. Every click writes `config/ycbotchallenge.json` and logs `option_toggle name= value=`; the controllers read their flag each tick, so a change applies at once, no restart. While the screen is open the bot lets go of every key and the hotkeys are ignored. No ModMenu or Cloth Config dependency.

### Menu manners (0.9.33)

A server sees only container click and close packets and their spacing — the cursor never moves inside a menu on the client — so the timing is the whole contract, and it used to differ per flow: the companion egg opens and the enchanter tab clicked in the same tick as the decision, three flows closed on three schedules, the rebirth-upgrade visit ignored breaks. `GuiHuman` is now the one way every menu is read, clicked and closed: a notice beat before every click (`guiClickMin/MaxMs`), a beat before every close (`guiCloseMin/MaxMs`), a pause between two menus (`guiBetweenMin/MaxMs`), a short read of a sub-menu (`guiReadMin/MaxMs`), the flow's own first look (`enchantLook*`, `rebirthLook*`, `companionLook*`). Every click and close is logged (`gui_click` with flow/slot/what, `gui_close`), and the rebirth-upgrade visit waits out a break like the others. Sixteen pre-0.9.11 knobs that nothing read are gone (`enchantVisitGap*`, `enchantSkipChance`, `enchantMaxBuysPerVisit`, `enchantMinEtaMs`, `enchantMaxBuysBetweenKills`, `enchantSkipFirstTabChance`, `upgradePeriod*`, `zoneEverySwords*`, `zoneOverSwordRatio`, `zoneMinSwordBuysThisZone`, `zoneReadyTtkMs`, `zoneMinReadiness`, `retryPriceGrowthPct`); an old config file still loads.

### Sword Skins scouting (0.9.33)

Inside the SWORD ENCHANTER an item "Swords" (`Click to view your swords`) opens the **Sword Skins** menu: each skin shows `Damage`, `Price`, `Tier: n/5`, and `EQUIPPED` / `LOCKED`. Every skin has five tiers; `/swordmax` climbs them and then promotes to the next skin, on the same ×3.5 ladder — so the equipped skin's Price is what the next `/swordmax` pays and a LOCKED skin's price is the promotion. At the end of an enchanter visit the bot clicks the item, reads the menu (`sword_menu`: skins, equipped, tier, damage, next price and its raw text) and closes it — whenever the sword price is unknown or only ladder-predicted or the tier is unknown, else with `swordMenuScoutChance` (0.35). A menu price becomes the sword target only within `swordMenuPriceBandPct` (30) of the known target or of a ladder step from the last price (`sword_menu_price verdict=match|ladder-match|no-reference|rejected`): the tooltip's suffix may be a font glyph the text component does not carry (`$139.880` for 139.88Q), and a mis-read must never override the ladder. The tier shows on the HUD sword row (`tier 3/5`, `~4/5` once a `/swordmax` moved it until the next read) and on the Y screen. Buying stays typed. The `/zones` menu (DUNGEON ZONES → DUNGEON STAGES, paged) is not automated: the typed probe already learns the next stage price.

### Ignored mobs

Each zone has an `[AFKMOB]` upgrade mob ("[AFKMOB] LVL7 Donkey ❤∞", right-click to upgrade): the same species as the real mobs, standing still, with infinite HP, so every other filter lets it through and the bot used to lock onto it. `ignoreMobPatterns` (default `["[afkmob]", "❤∞"]`, substrings or `/regex/`, case-insensitive) makes such nameplates untargetable; the first sighting of each is logged once as `target_ignored` (with `via`).

**Stay in your zone (0.9.27).** `targetRange` (50) reaches the neighbouring zones and the dominant-pack filter drops out exactly while the local pack is thin — after a respawn broadcast or a zone advance — so the nearest anything won: the 20:35 log has a Chicken picked in zone 7 with a 132° turn 2.7 s after "All mobs have been respawned", and a stray neighbour species in every zone of the session (Villager in lvl9, Horse in lvl7, Mooshroom in lvl8 …). The plate carries the stage (`LVL7 Donkey ❤69B`; the plate regex now parses the untagged shape too, so `tag_intent`/`kill` carry `level` for every mob), and `targetZoneLevelOnly` (default on) makes a mob whose plate level differs from the boss-bar-confirmed zone level never a candidate — mispicks included. The confirmed level is cleared by a teleport or rebirth and set again by the first hit's bar, so the filter is off for those seconds and the dominant filter carries them as before; a respawn broadcast keeps it. Rejections log `target_offzone` once per mob (plate level vs zone level).

**0.9.26: the plate was never the name.** The 19:26 log (and every log before it) shows `target_ignored` firing zero times and no `tag` ever carrying a rarity: on EnchantedMC a mob's plate is not its entity name but a text display riding the mob or floating above it (the AFK Mooshroom carries three lines: `⟡332.12B⟡`, `[AFKMOB] LVL9 Mooshroom ❤∞`, `RIGHT CLICK TO UPGRADE`), so the 0.9.14 rule read a name that was never there and a `target_mispick` roll walked straight into the AFK mob. Three layers now: (1) **holograms** — the mob's passengers, else every plate entity within `nameplateHologramRadiusBlocks` (0.9) horizontally and up to 3.5 blocks above, are its nameplate lines; any line matching `ignoreMobPatterns` makes it untargetable (`target_ignored via=hologram`), and the same lines now feed rarity, level and the log's mob names; (2) **boss bar** (`ignoreByBossBar`) — after the first hit, when every bar mentioning the mob matches (`[AFKMOB] LVL9 Mooshroom`), the target is dropped for the session (`via=bossbar`) instead of being cooked to the timeout; (3) **by hand** — **Ctrl + toggle key** while looking at a mob marks it ignored (`via=manual`, chat notice), persisted by kind and position in `config/ycbotchallenge-ignored.json` and matched within `manualIgnoreRadiusBlocks` (1.5) so it survives rejoins and respawns; Ctrl + toggle on a marked mob removes the mark (`target_unignored`). With nothing in reach the nearest mob within `manualIgnoreAimDeg` (4°) of the crosshair is taken.

### Stop protocol + TTK

- **Stop protocol** (`stopProtocolEnabled`): teleport first, radar second. A single-tick displacement over `teleportThresholdBlocks` (12) stops instantly and *arms* the player radar for the rest of the session (`playerRadarArmAfterTeleport`, default on). Our own `/zone max` advances are exempt — they neither stop the bot nor arm the radar, so zone NPCs and AFK fixtures can never trip it mid-grind. Since 0.9.20 an unexplained teleport first holds still for `teleportExplainGraceMs` (6s): the server's `/autorebirth` teleports with no command of ours to arm it (17:03 log: 28.23Q → 32-block teleport → money 0 at +0.4s → rebirth counter at +4.4s, and the bot had already stopped), so a rebirth signal inside the grace — the `[!] You have successfully rebirthed.` line (`rebirthChatPatterns`), the money collapse, the sidebar counter, or the balance having reached a known rebirth cost — turns it into a rebirth teleport (`teleport_explained`, then the usual post-rebirth settle); no signal = `stop_protocol` as before. One rebirth's three signals reset the economy once (`economy_reset_dedup`). Once armed, a non-whitelisted player continuously within `playerRadarRadius` (48) for `playerRadarDwellMs` (5 s) stops the bot — a lingering stranger is staff. `playerRadarWhitelist` (matches any line of multi-line plates, §-codes and small caps normalized) and `playerRadarIgnoreStationaryMs` remain as second-line defenses; first sightings log as `radar_seen`. Fires a `stop_protocol` event and a red chat notice.
- **TTK** now measures connect → boss-bar-gone (server-authoritative death) instead of client-side entity removal, which ghosts were glitching. A bar that vanishes under `barVanishMinCookMs` with the entity still alive is held for `instantKillConfirmMs` (1.5s, 0.9.21): the entity vanishing or the sidebar money rising after the tag makes it a kill (`kill via=instant`, TTK = the bar's lifetime; `via=death+money` when the bar never rendered at all), nothing makes it a tag that didn't stick. Before this, zone 1 after a rebirth (one-click chickens, bar alive one tick, money a second later) filed 36 of 39 tags as retags, froze the kill counter and the TTK median at the two slow first kills, kept the zone gate closed, and bought 31.5M of swords instead of the first zone.
- **Rarity HP scaling**: kill durations are normalized by `rarityHpScale` (default RARE +15%, EPIC +30%, LEGENDARY +40%) before entering the TTK window, so a tanky legendary doesn't read as slow farming. Mobs with no rarity tag get 1.0 (the nameplate parser can't see a tag that isn't there).

- `zoneMaxTtkMs` (10000) — zone gate base: no `/zone max` while the effective TTK is above this stage's rolled patience or unknown. 0 disables.
- `rebirthSignalWaitMs` (6000) / `rebirthProbeMaxRetries` (1) — after the diamond click, how long to wait for the server's fail line or rebirth signal; how many times per session a probe that never reached the GUI may be re-typed. A probe that reached the GUI and went unanswered is never re-typed.
- `zonePatienceMinMult` / `zonePatienceMaxMult` (0.6 / 1.6) — patience roll band around `zoneMaxTtkMs`, re-rolled per zone change and enable (`zone_patience`). Both 1 = the fixed line.
- `predictedTtkMaxAgeMs` (4000) — a DPS prediction older than this is dropped; the kill median outranks any prediction once three kills of the stage have landed. 0 keeps every prediction.
- `evalFallbackMs` (30000) / `evalOnMoneyIncrease` (true) — upgrade evaluation triggers beyond kills: a timer backstop and any sidebar money increase.
- `cooldownRelaxBalanceMult` (3.0) — the 60s per-kind cap collapses to `commandCooldownMs` while balance ≥ this × the kind's last price. 0 disables.
- `cookStallMs` (15000) — `maxCookMs` only abandons a mob whose boss-bar HP has not dropped for this long.
- `resetTtkOnEnable` (true) — enabling the bot clears the TTK window (`ttk_reset`) so the zone gate re-measures wherever you are: after `/spawn`, a manual zone hop, or an AFK gap. Learned prices are kept.
- `zoneMinStageKills` (1) — kills on the stage a `/zone max` landed on before the next zone buy (0.9.33); 0 chain-buys on the teleport.
- `ttkKeepOnReenableMs` (60000) — a toggle within this on the same stage keeps the kill window and the patience roll (0.9.33).
- `gateUsesPrediction` (false) — legacy: let the DPS prediction fill an UNKNOWN gate (the 0.9.23–0.9.32 rule).
- `stageProbeCommonKills` (1) / `stageProbeRarityPenaltyBlocks` (30) — the first target on a fresh stage is a common mob (0.9.33).
- `learnObservedUpgrades` (true) / `offBotLogIntervalMs` (30000) — learn manual upgrade responses; throttle bot-off balance/income rows (0.9.33).
- `hudShowPlan` (true) / `hudShowModules` (false since 0.9.33) — the plan row and the module chip row.
- `companionMaxRebirthDelayMin` (3.0) — the floor of the persistent payback budget: how far a batch may push the rebirth out before any cycle has been measured (0.9.35, floor since 0.9.36).
- `companionCyclePriorMin` (45) / `companionPaybackFraction` (0.5) — the cycle length assumed until `cycle_end` measures one, and the share of what the next cycle saves that a batch may spend delaying this rebirth (0.9.36).
- `companionStageSettleKills` (3) / `companionStageSettleMs` (120000) — the income figure is this stage's own after either; gates only the persist rule (0.9.36).
- `companionMaxVisitsPerStage` (2) — the real visit budget; `companionMaxVisitsPerRebirth` (8) is only a runaway backstop (0.9.35).
- `companionMaxBalancePct` (40) — manual visits only since 0.9.36.
- `companionPriceGrowth` (52.2) — the egg ladder, so a stage never visited still has a batch price (0.9.35).
- `companionGainPrior` (1.5) / `companionGainMin` (0.8) / `companionGainMax` (3.0) / `companionGainWindowMs` (180000) — what one batch is worth, and the band it is learned inside (0.9.35; floor 0.8 since 0.9.36).
- `zoneBackMeasureEnabled` (true) / `zoneBackMargin` (1.5) / `zoneMoneyGrowthPrior` (20) — the `zone_back_candidate` measurement: log when the previous stage would out-earn a fresh one by the margin; nothing is sent (0.9.36).
- `companionFuseEnabled` (true) / `companionFuseMinGroup` (5) / `companionFuseAllPattern` — fuse before Equip Best when the storage holds a group (0.9.37).
- `companionObservedTolerancePct` (3) — a sidebar drop this close to 1/3/10 eggs at the stage price is a hand purchase (0.9.37).
- 0.9.39 companion fixes: fusable groups are counted in the storage only (the server fuses the backpack, not what is worn — the 02:51 log clicked Fuse All on 7 Eagles with three equipped and got "No Companions fuseable"); after Fuse All the server re-renders the fusion menu, which used to abort the visit as `screen-open` before Equip Best ran and left the new eggs unequipped — a menu of ours is now closed again and waited out; a visit that already opened eggs counts as a visit when it aborts (three aborted lvl19 visits each bought again because none was counted); `companion_fuse` carries `fused` and the server's `verdict`.
- `ggPerkEnabled` (true) — the perk-pull replies on their own Y-screen switch ("GG on perk pulls"); `ggEnabled` covers both (0.9.39).
- `ggEnabled` (true), `ggWavePatterns` / `ggWaveChance` (0.85) / `ggWaveMessages` / `ggWaveDelayMin/MaxMs` (300–1500) / `ggWaveWindowMs` (6000) / `ggWaveBlockMs` (2000), `ggPerkPatterns` / `ggPerkChance` (0.5) / `ggPerkMessages` / `ggPerkDelayMin/MaxMs` (2000–8000) / `ggPerkWindowMs` (20000), `ggMinGapMs` (60000) — the gg replies (0.9.37).
- `stageProbePredictedMult` (3.0) — a fresh-stage prediction this far over the patience reads HARD at once (0.9.37).
- `buyNoticeSnowballMin/MaxMs` (500–3000) / `serverQuoteRelaxMs` (300000) — the snowball notice roll and how long a server-quoted price beats the per-kind cap (0.9.37).
- `approachClickMaxAimDeg` (25) / `noConnectTimeoutMs` (4000) / `noConnectIgnoreAfter` (2) — the approach-swing aim gate and the no-connect escape (0.9.37).
- `bossEventEnabled` (true), the `boss*Pattern`s, `bossScanRadius` (12), `bossStandTolerance` (0.8), `bossWalkTimeoutMs` (30000), `bossEventStartGraceMs` (8000), `bossEventMaxMs` (300000), `bossNoProgressMs` (4000), `bossMaxRescans` (3), `bossMarkerMoveHits` (10), `bossMarkerMoveBlocks` (1.5), `bossClickCpsMin/Max` (2.5/3.5), `bossRespectVanillaCooldown` (false), `bossHitLogEvery` (10), `bossMaxConsecutiveAborts` (3) — the zone boss (0.9.38).
- `guiClickMin/MaxMs` (250–900), `guiCloseMin/MaxMs` (400–1500), `guiBetweenMin/MaxMs` (1500–3500), `guiReadMin/MaxMs` (300–900), `companionLookMin/MaxMs` (600–3500) — the one menu timing policy (0.9.33, Menu manners).
- `swordMenuScoutEnabled` (true) / `swordMenuScoutChance` (0.35) / `swordMenuPriceBandPct` (30) and the `swordSkin*` patterns — Sword Skins scouting (0.9.33).
- `scoreboardSnapshotMs` (5000) — how often the canonical multi-currency snapshot is published.
- `sidebarCurrencies` — names parsed from the sidebar (`money`, `souls`, `essence`, `shards`, `credits`).
- `ttkWindowKills` (8) — rolling window for the median TTK. A per-zone baseline is snapshotted a few kills after each zone change and logged via `zone_benchmark` events.
- `upgradeMinIntervalMs` (60s) — backstop ceiling between sends of the same kind. Unaffordable evals do not send.
- `buyNoticeDelayMinMs/MaxMs` (2–8s) — humanized delay between becoming affordable and typing.
- `successSilenceMs` (3000) — no fail line this long after a send = purchase succeeded.
- `retryPriceGrowthMinPct/MaxPct` (0.2–0.8) — unknown-price retry fires when the balance passes old price × (1 + a growth rolled per success).
- `upgradeResponseWindowMs` (4000) — replies are only attributable to our send within this window.
- `expectedTeleportAfterZoneMs` (8000) — stop-protocol exemption after our own `/zone max`.
- `upgradeSpendSettleMs` (2500) — ignore the sidebar this long after a buy (board lags after spending).
- `minKillsAfterAffordable` (0) — extra kills after the balance first covers the target before typing (at a 90s TTK, 1 held 2.75B against a 2.5B stage for a minute and a half).
- `summaryHeaderPattern` / `summaryMoneyPattern` — the `Reward Summary: (60s)` / ` + 17.19B Money` income-rate lines.
- `upgradeFailPatterns` / `upgradeNeedAmountPattern` — anchored fail lines and the gap-amount extractor.
- `upgradeSuccessPatterns` — anchored success lines; the sword pattern's `amount` group is the exact price paid.
- `suffixScales` — manual amount suffixes, e.g. `{"Sx": 1e21}`; they win over learned and built-in scales. Only K M B T Q QQ are built in; the rest is learned.
- `suffixLearningEnabled` (true) / `suffixCrossingMaxJump` (20) — learn new suffixes from sidebar rung crossings (persisted in `config/ycbotchallenge-suffixes.json`); the largest jump between two polls still taken as the next rung.
- `upgradeMaxedPatterns` — anchored response lines meaning a kind is fully upgraded (it is then never sent again).

### Ninja humanization

`ninja: true` (default) enables the realism layer; `false` restores the old mechanical behavior. All rates are tunable:

- Timing: soft bounds instead of hard clamps (`softClampMarginPct`), rare heavy-tail pauses (`tailChancePerDelay`), per-session bounds jitter (`sessionJitterPct`), fatigue drift (`fatiguePerHour`), and long distractions (`distractionChancePerMinute`, 2–30 s by default).
- Mouse: fast one-shot flicks with a big swoopy curve (~350 ms for a 30° snap at `aimAgility` 1.0 — the old default `0.4` is auto-migrated). Bump size is tunable via `curveBumpMinPct/MaxPct` (7–22% of flick distance by default), sparse idle tremor only, and small flick-tempo regimes (`agilityRegimes`) so no single Fitts regression fits. `mouseChaining` (mid-path re-targets) exists but is off by default — it reads as servo ticking.
- Mistakes: `misclickChance`, `wrongTargetChance`, `sprintHitChance`, `typoChancePerChar` (typo + backspace while typing commands).
- Session theater: `breaksEnabled` with `focusMinutesMin/Max` and `breakMinutesMin/Max`. Focus counts only time the bot is actually running (never wall clock while it is off), and `breaksResetOnToggle` (default on) makes any toggle, stop or captcha pause end the break and start a fresh focus block — the HUD shows `on break — 142s left (toggle to skip)`. Logged as `focus_start` / `break_start`.
- `movingTargetPolicy`: `ignore` (default, ghost filter untouched) or `sometimes` (`movingTargetAttackChance`).

## Captcha auto-solve (QwenCloud qwen3.6-flash since 0.9.32; local Qwen3-VL optional)

**0.9.32: the native map, one read, a cloud model.** Four certified captures (KrA, p8b, pnGe, udWn) and a night of benching settled it. Every reader — the local Qwen3-VL 4B ballot, the 8B in 4-bit, and QwenCloud's qwen3.6/3.8-flash, 3.7-plus and 3.8-max — read `udWn` as `uaWn`/`ualWm` when given an **upscaled** render, and image cleaning or per-glyph reads made things worse. Sent the map's own 128×128 pixels with no upscaling, `qwen3.6-flash` reads all four in one greedy call each (164 input tokens, 3–5 s). So the defaults are now: `captchaVlmEndpoint` = QwenCloud's token-plan OpenAI-compatible endpoint, `captchaVlmModel` = `qwen3.6-flash`, `captchaMapScale` 1, `captchaVoteRenders` `["x1"]`, `captchaVoteTemperature` 0 (one read, no ballot), `captchaVlmThinking` false (`enable_thinking=false` in the request), and the key from env `YCBOT_VLM_KEY` or the one-line file `~/.ycbot_vlm_key` (never logged, never in the repo). A rejection (map still in hand after `captchaMapHeldRejectMs`) sends the look-alike second guess as before, then stops at `captchaMaxAnswers`. `python tools/captcha_bench.py --single` runs the same one-read test against the fixtures (`--real` for the bot's own captures only; `YCBOT_VLM_URL`/`YCBOT_VLM_MODEL` select the endpoint). Cost at one captcha an hour is a fraction of a cent a day; note that the provider's terms, like every provider's, forbid CAPTCHA circumvention — the account is the user's risk. The local reader stays available: point the endpoint back at vLLM (`http://127.0.0.1:8000/v1/chat/completions`, `Qwen/Qwen3-VL-4B-Instruct-FP8`, scale 4, the x4bil… render list, temperature 0.6).

EnchantedMC's captcha is a **filled map put in your hand** (2026-09-02: it landed in hotbar slot 9 and was selected): the map shows a short random string in colored pixel letters over noise specks, and you type it in chat, **case-sensitive**, within ~30 s. No chat line announces it, so 0.9.13 watches the hands and hotbar (`CaptchaDetector`): a map appearing in a slot that had none fires after `captchaSignalConfirmMs` (`captcha_detected source=held-map|hotbar-map`), and that map id is muted until it is gone. Maps already there at enable time are snapshotted (`hotbar_snapshot`) and never count; `captchaMapAnySlot: false` limits it to the hands. The old triggers stay: a chat line matching `captchaChatPatterns` (never overlay text, and never a player/broadcast line with `»` or a `[rank]` prefix: on 2026-09-03 another player's "next time just captcha him" made the bot screenshot the arena and type a guess into public chat; such matches are now logged `captcha_chat_ignored`) or a server-opened container GUI. Soft hints (`captchaChatHintPatterns`, logged `captcha_hint`) only make a map confirm instantly; they never trigger alone.

With `captchaAutoSolve: true` (default) the bot then:

1. waits `captchaSettleMs`, then reads the map's **own 128×128 pixels** from any hand/hotbar slot (or the nearest item-frame map within `captchaMapSearchRadius`), waiting up to `captchaMapDataWaitMs` for the map-data packet; **no map anywhere = not a captcha the model can read: `captcha_failed stage=capture why=no-map` and a pause for you** (`captchaCaptureMode` `"map"`, the default since 0.9.16; `"auto"` re-enables the HUD-less screenshot fallback downscaled to `captchaScreenMaxPx`, `"screen"` always screenshots). Every capture is dumped to `ycbotchallenge-logs/captcha-<time>-<mode>.png` (`captchaDebugPng`) as the fixture for prompt tuning;
2. POSTs it to `captchaVlmEndpoint` (QwenCloud's `qwen3.6-flash` by default since 0.9.32; the key comes from env `YCBOT_VLM_KEY` or the one-line file `~/.ycbot_vlm_key`, never the config. Point it at `http://127.0.0.1:8000/v1/chat/completions` for the local vLLM reader) with `captchaMapPrompt`, which asks for a **JSON array of single characters** and says the string is not a word; the answer is assembled in code (`ChatClassifier.parseAnswerArray`), case kept (`captchaPreserveCase`);
3. pauses `captchaAnswerDelayMin/MaxMs` (reading it), then **types the answer through the chat screen** like every other send (`ChatTyper`: per-key log-normal delays, `typoChancePerChar` typo + backspace), `captcha_answer typed=true`;
4. resumes when a `captchaSolvedPatterns` line arrives or after `captchaVerifyWaitMs` of silence. On a `captchaRetryPatterns` rejection the second (and last, `captchaMaxAnswers`) guess is the reading with the case of one look-alike letter flipped (`captchaCaseAmbiguous`), built in code with no second model call; a second rejection stops and pauses for you (`captcha_pause reason=answers-exhausted`). Never spam.

**0.9.26: a running ballot, a map that stays is a no, and the typo that was not a glitch.** The 19:43 log: the bot read `Kra` from its x4 render, typed it, heard nothing for 12 s, declared the captcha solved and resumed; the answer was `KrA` (Drew, by hand). Bench on the exact image the bot sent: x4 smoothed reads `Kra` 12 of 12 times at any temperature, x3 and x2 read `KrA` 8 of 8, and a "which case is it" prompt still says `Kra` — the model is consistently wrong on that one rendering, and the cross-check that had it right was waiting for a rejection line the server never sends (`latest.log` 13:43: silence after a wrong answer; silence after a right one too, the map just leaves the hand). Three changes. (1) **Ballot**: the map is rendered once at every scale in `captchaVoteRenders` (`x4bil, x3bil, x2near, x5bil, x3near`) and one background reader reads them in turn, the schedule at temperature 0 then at `captchaVoteTemperature` (0.6), up to `captchaVoteMaxReads` (12); every reading is a vote, the first answer is the leader once the human reading pause has passed and `captchaVoteMinReads` (3) votes are in, voting continues while the answer is verified, and a rejection sends the best-supported alternative (`captcha_vote` at each send and rejection with the tallies; `captcha_candidates via=ballot`). On the certified captures the vote is right on all of KrA, p8b and pnGe where the primary render alone was wrong on KrA; `python tools/captcha_bench.py --vote` replays it. (2) **Map still held = rejected** (`captchaMapHeldRejectMs`, 7 s): the answered map still in a hand or the hotbar that long after the answer logs `captcha_map_persists` and the next candidate goes out — the `captchaMaxAnswers` (2) cap still holds; and any re-detection of the map already answered from continues with the remaining candidates instead of re-reading it. (3) **Typing**: a typo used to advance past the intended character, so the field genuinely read `/zne max` while the original string was sent — what was on screen was not what was sent. The keystroke rule is now a pure, tested step (`ChatTyper.step`): the slip goes in, the backspace takes it out, the intended character follows; the field ends exactly as the command, and that is what is sent (`typedMismatch:true` in `upgrade_send` / `captcha_answer` if it ever differs). The `KrA` capture is certified as `tools/captcha-fixtures/KrA-map128.png`.

**Certified fixtures and the bench (0.9.22).** Every captcha whose answer is known lives in `tools/captcha-fixtures/` (`fixtures.json`: file, answer, provenance) — today `pnGe` (2026-09-02 screenshot, board crop on the 128 px map grid) and `p8b` (2026-09-03 17:38, the bot's own map capture; it read `pBb` at the 256 px render and Drew confirmed `p8b`). `python tools/captcha_bench.py` runs prompts × render scales × sampling against them with the mod's own answer parsing and prints greedy and sampled hit rates plus the best two-render pairs; run it before touching `captchaMapPrompt`, `captchaMapScale` or `captchaMapSmooth`, and add each new capture with its accepted answer. The 12-sample run that set 0.9.22: shipped prompt at ×4 smoothed 2/2 greedy, 23/24 sampled; ×2 nearest 1/2 (p8b → pBb every time); ×4 nearest 0/2; a digits-coaching prompt over-corrects (`8bb`). Hence `captchaMapScale` 4 (migrated from 2), a `captchaSecondScale` (3) cross-check whose disagreement is the second guess, and `captchaLookalikes` (`B8,O0,S5,Z2,…`) for the second guess when both agree. The server's `Please enter the captcha on the map.` is its re-prompt after a wrong answer (it arrived 5 s after `pBb`), so it is a `captchaRetryPatterns` signal, `captchaVerifyWaitMs` is 12 s so it lands inside the window, and a chat re-prompt for a map already answered continues with the next guess (`captcha_reprompted`) instead of re-reading it. The reading pause is 2.5–5 s (`captchaAnswerDelayMin/MaxMs`).

**Image size matters more than the prompt.** Bench 2026-09-03 on the `pnGe` capture with the local 4B model (0.1–0.4 s per call): the JSON-array prompt read it right on every input from 192 px to 1024 px wide (6/6 samples at temperature 0.7), while the native 1605 px screenshot and a 128 px map upscaled ×4 with nearest neighbour misread (`pinge`, `prGe`) and ×6 was garbage; ×2 nearest or ×4 bilinear read right. Hence `captchaMapScale` 2 (migrated from 4) with `captchaMapSmooth` for anything larger, and the screenshot cap. Word-shaped prompts (`ANSWER: <word>`) returned `pinc`/`pInGe`, and a per-letter colour enumeration inserted a phantom `i` every time: ask for characters, not words.

**Reader health, hedged reads and the budget (0.9.34).** The 16:07:13 line on 2026-09-04:

```json
{"type":"vlm_health","online":false,"error":"HTTP 503","url":".../compatible-mode/v1/models","changed":true}
```

One transient 503 from QwenCloud, recovered 97 s later — and in that window the bot would have refused the next captcha outright (`captcha_pause reason=vlm-offline`) *without even capturing the map*, then waited up to `captchaVlmHealthIntervalMs` (5 min) to look again. Three things were wrong. The probe GETs `/v1/models` while a solve POSTs `/chat/completions`, so it gated on a route it never uses. It called any non-200 death, while the solve path only ever treated a *connect* failure as fatal — the same 503 mid-solve was a shrug. And it tripped on one sample, at roughly one captcha an hour, where a circuit breaker has no load to shed and can only produce false negatives. The HUD then said `captcha VLM: offline — start vLLM`, naming a server the mod had not used since 0.9.32.

So: **health is telemetry now, not a gate.** A captcha is always attempted. `vlm_health` carries `reach` — `online`, `degraded` (it answered: 5xx, 4xx, a read timeout) or `unreachable` (no usable connection: refused, DNS, closed channel — the signature a stopped local vLLM gives in 2–5 ms). Only `unreachable` reddens the HUD, and the line names the host from `captchaVlmHealthUrl` rather than assuming vLLM.

The resilience moved into the read itself, as **hedges rather than retries**: read A fires the moment the map is captured, read B `captchaHedgeMs` (3 s) later **without waiting to see whether A failed**, both voting into the same 0.9.26 ballot. A serial retry cannot fit — it costs a full timeout before the second try starts — but a hedge is free, because 3 s lands read B inside the 2.5–5 s `captchaAnswerDelay` reading pause the bot already takes. A read that comes back failed pulls the next hedge forward instead of waiting out the stagger (`captcha_hedge_accelerated`); a third read fires only on a failure or a split ballot (`captchaHedgeMax` 3). `captchaTimeoutMs` drops 20 s → **8 s** (a read benches at 3–5 s; at 20 s one slow call ate two thirds of the window). Reads keep voting through the verify window, so a rejection still has its alternative with no fresh call.

Everything is bounded by one clock: **`captchaBudgetMs` (25 s)**, stamped at detection, against the server's ~30 s window. Past it the bot hands over (`captcha_pause reason=budget`) rather than typing an answer that would land too late — the remaining ~5 s is yours. No read starts that cannot finish and still leave room to type.

What this does **not** cover: hedging the same endpoint decorrelates per-request failure (one bad instance, queueing variance, a dropped connection, a transient 503), not correlated failure (the provider regionally down, the key exhausted, your network gone) — there all hedges fail together. That case wants a second endpoint, which is deliberately not built yet; `fireHedge` picks its target per read, so it is the seam to add it at.

Check the endpoint by hand with (the local reader's URL shown; use `captchaVlmHealthUrl` for whatever is configured):

```bash
curl http://127.0.0.1:8000/v1/models
```

**Local reader (optional).** Drew's serve line (WSL2, 3080 Ti): `vllm serve Qwen/Qwen3-VL-4B-Instruct-FP8 --host 0.0.0.0 --port 8000 --max-model-len 4096 --gpu-memory-utilization 0.75 --max-num-seqs 2 --limit-mm-per-prompt '{"image": 1, "video": 0}'`. A cloud OpenAI-compatible endpoint needs only `captchaVlmEndpoint`/`captchaVlmModel`; the key is read from `YCBOT_VLM_KEY` or `~/.ycbot_vlm_key` (never the config file). Note the local ballot defaults (`captchaMapScale` 4, the `x4bil…` render list, `captchaVoteTemperature` 0.6) are the 0.9.26 setup and must be set by hand — the shipped defaults are the cloud one-read config.

Events: `captcha_detected`, `hotbar_snapshot`, `captcha_captured` (mode, where, px, png), `captcha_candidates` (raw reply), `captcha_hedge` (n, render, temp, budgetLeftMs), `captcha_hedge_accelerated`, `captcha_vote` (reads, tallies, hedges, lastReadMs), `captcha_answer` (typed, typos), `captcha_budget_spent`, `captcha_solved` / `captcha_failed` / `captcha_pause` (reason, incl. `budget`), `captcha_hint`, `chat_raw` (unclassified server lines, ≤ `chatRawPerMinute`), `vlm_health` (reach), `vlm_model_auto`. Set `captchaAutoSolve: false` for the old hard pause.

The Sonar defaults (chat-triggered captcha, 3–4 lowercase letters, `ANSWER`/`ALT` lines with `captchaPrompt` and `captchaRetryPrompt`) are kept for chat/GUI-triggered captchas with no map in reach.

## How it detects progression

Rebirths: sidebar counter going up = rebirth; counter dropping = reset = ascension (plus chat-message patterns as a second signal, same for prestige). Boosts: boss bar titles, tracked as start/end and stamped into every event's context. Balances: the sidebar rows (`balance` on change, `scoreboard_snapshot` every 5s); the reward summary feeds only the income rate. Zone: own teleport, boss-bar mob level, respawn broadcast or a sidebar Zone row. Log events: `scoreboard_snapshot`, `balance`, `income`, `income_summary`, `upgrade_plan` / `upgrade_skip` (with `via` = kill/money/timer, `decision`, `reason`, `gate`/`gateVia`, `stageKills`, `ttkMs`, `zoneGate`, `swordFloor`/`zoneFloor`, `zoneGapVia`), `zone_gate_hard`, `upgrade_send`, `upgrade_chat`, `upgrade_result` (`paid`, `paidPending`, `extraLevel` for multi-level `/swordmax`), `upgrade_paid` / `upgrade_paid_missed`, `upgrade_observed`, `upgrade_maxed`, `upgrade_response_raw`, `target_common_first`, `gui_click` / `gui_close`, `sword_menu` / `sword_menu_price` / `sword_menu_skip`, `companion_egg_saved`, `companion_gain`, `companion_ratio`, `companion_price_ratio`, `bot_on` / `bot_off` (every row carries `bot`), `zone_benchmark`, `zone_teleport`, `zone_change`, `boss_level`, `ttk_reset`, `economy_reset`, `target_abandoned` (`sinceHpDropMs`), `focus_start`, `enchant_*` (see Enchants during long kills) (plus ninja noise: `misclick`, `target_mispick`, `sprint_hit_slip`, `distracted`, `break_start`, `aim_regime`).

## Building from source

```bash
gradle build          # Gradle 8.14+, JDK 21
# jar lands in build/libs/
```

Note: `fabric-loom` is pinned to `1.13-SNAPSHOT` because newer loom (1.14+) requires Gradle 9. If you're on Gradle 9+, you can bump loom freely.
